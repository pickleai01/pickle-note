# 피클노트 (Pickle Note) v2.0.0 — 기능 명세서

> 작성일: 2026-02-22
> 목적: 현재 구현된 기능의 정확한 파악 및 경쟁력 검증

---

## 1. 제품 요약

| 항목 | 내용 |
|------|------|
| **제품명** | 피클노트 (Pickle Note) |
| **버전** | 2.0.0 |
| **형태** | Chrome 확장 프로그램 (Manifest V3) |
| **핵심 가치** | YouTube 영상 → NotebookLM 자동 분석을 **원클릭**으로 |
| **기술 스택** | Vanilla JS, 인라인 CSS, 프레임워크/번들러 없음 |
| **가격** | 무료 |
| **데이터 전송** | 없음 — 100% 로컬 브라우저 내 동작 |

---

## 2. 핵심 기능 목록

### 2.1 원클릭 자동화 파이프라인

YouTube 페이지에서 버튼 하나로 아래 5단계가 **전자동** 실행:

| 단계 | 동작 | 구현 위치 |
|------|------|-----------|
| 1/5 | NotebookLM 탭 열기 + 소스 추가 버튼 클릭 | background.js → content.js `runAutomation` |
| 2/5 | "웹사이트" 선택 → YouTube URL 자동 입력 | content.js `runAutomation` (textarea value setter) |
| 3/5 | "삽입" 버튼 클릭 (소스 등록) | content.js `clickButtonByText` |
| 4/5 | 선택한 모드에 맞는 프롬프트 자동 입력 | content.js `buildPrompt` → chatInput |
| 5/5 | 전송 버튼 클릭 (또는 Enter 키 fallback) | content.js submit button / keydown |

**자동화 중 사용자 경험:**
- blur 배경 + 스피너 오버레이 (현재 단계 표시)
- 프롬프트 미리보기 박스 (입력될 프롬프트 확인 가능)
- "숨기기" 버튼으로 오버레이 수동 해제 가능
- 완료 시 시스템 알림 (chrome.notifications) + 2.5초 후 오버레이 자동 해제

---

### 2.2 8가지 분석 모드

| # | 모드 | 코드명 | 설명 | 프롬프트 특징 |
|---|------|--------|------|---------------|
| 1 | **전체 요약** | `FULL_DOC` | 한 줄 요약 + 핵심 내용 + 기억할 포인트 + 한계/주의 | 주제별 소제목 구조화 |
| 2 | **핵심 Q&A** | `FAQ` | 질문-답변 형식 추출 | What/Why/How 유형 다양화 |
| 3 | **팟캐스트** | `PODCAST` | 2인 대화 스크립트 (수진/민호) | 전문가+청취자 구어체 대화 |
| 4 | **원본 보기** | `FULL_TEXT` | 전문 속기 (요약 없이 전체 기록) | 소주제 헤더, 화자 구분 |
| 5 | **학습 노트** | `STUDY_NOTE` | 개념 정리 + 암기 포인트 + 연습문제 | 시험 대비 최적화 |
| 6 | **회의록** | `MEETING_MEMO` | 개요 + 논의 + 결정사항 + 액션아이템 | 공식 회의록 형식 |
| 7 | **커스텀 1** | `CUSTOM_1` | 사용자 정의 프롬프트 | 최대 5,000자 |
| 8 | **커스텀 2** | `CUSTOM_2` | 사용자 정의 프롬프트 | 최대 5,000자 |

**각 프롬프트 공통 규칙:**
- 지정 언어로 작성 (전문 용어 병기)
- 영상에 없는 내용 추가/추측 금지
- 수치, 고유명사, 예시 원본 그대로 반영

---

### 2.3 옵션 시스템

#### 언어 (4개)
| 값 | 표시 |
|----|------|
| `ko` | 한국어 |
| `en` | English |
| `ja` | 日本語 |
| `zh` | 中文 |

#### 분량 (3단계)
| 값 | 표시 | 예: 전체 요약 | 예: Q&A | 예: 팟캐스트 |
|----|------|---------------|---------|-------------|
| `short` | 간략 | 2~3개 주제, 각 1~2줄 | 3개 | 4~5쌍 |
| `medium` | 기본 | 3~5개 주제, 각 2~4줄 | 5개 | 6~8쌍 |
| `detailed` | 상세 | 5~7개 주제, 각 4~6줄 | 7개 | 8~10쌍 |

#### 기존 소스 삭제
- 체크 시: 기존 소스 전부 삭제 → 채팅 기록 삭제 → 노트북 제목 변경 → 새 소스 추가
- 미체크 시: 기존 소스 유지, 새 소스만 추가

---

### 2.4 노트북 관리

| 기능 | 설명 | 동작 조건 |
|------|------|-----------|
| **자동 스캔** | NotebookLM 홈페이지 방문 시 노트북 목록 자동 수집 | 홈페이지 로드 시 |
| **수동 새로고침** | 새로고침 버튼으로 목록 재스캔 | NotebookLM 페이지에서만 |
| **노트북 선택** | 목록에서 클릭하여 대상 노트북 지정 | 항상 |
| **노트북 생성** | 노트북 미선택 시 자동으로 새 노트북 생성 | YouTube에서 버튼 클릭 시 |
| **노트북 삭제** | 목록의 휴지통 아이콘으로 삭제 | 홈페이지/상세/외부 모두 가능 |
| **자동 제목 설정** | 새 노트북 생성 또는 소스 삭제 시 YouTube 영상 제목으로 자동 변경 | 자동화 실행 시 |
| **선택 상태 보존** | 목록이 비어있어도 `selectedNotebookId` 유지 | 스캔 전 상태 |
| **URL 직접 입력** | 설정에서 노트북 URL 수동 입력 가능 | 설정 패널 |

**노트북 감지 우선순위:**
1. 열린 탭에서 자동 감지 (`tab`)
2. 목록에서 선택 (`list`)
3. 마지막 방문 노트북 (`storage`)
4. 수동 설정 (`manual`)

---

### 2.5 노트북 삭제 자동화

4단계 자동 삭제 프로세스:

| 단계 | 동작 |
|------|------|
| 1/4 | 홈페이지에서 해당 노트북 카드 탐색 (ID 기반) |
| 2/4 | 카드의 더보기(⋮) 버튼 클릭 |
| 3/4 | "삭제" 메뉴 항목 클릭 |
| 4/4 | 확인 다이얼로그 대기 (최대 5초 폴링) → 확인 클릭 |

**안전장치:**
- 확인 버튼 못 찾으면 즉시 실패 리턴 (거짓 "완료" 보고 방지)
- 삭제 후 DOM 검증: 카드가 실제로 사라졌는지 최대 5회 확인
- 삭제 미확인 시 "삭제 확인 필요" 경고
- 삭제 후 목록 자동 재스캔

**라우팅 로직:**
- 홈페이지에 있을 때 → 직접 Content Script로 `DELETE_NOTEBOOK` 전송
- 상세페이지 또는 외부 → Background로 `NOTEBOOKLM_DELETE` → 홈페이지 이동 후 삭제

---

### 2.6 소스 삭제 + 채팅 기록 삭제

**소스 삭제 (`deleteAllSources`):**
- 최대 20라운드 반복
- 더보기 버튼 순회 → "삭제" 메뉴 탐색 (채팅 메뉴 제외 필터: `text.includes('채팅')` 제외)
- 각 소스 개별 삭제 (확인 다이얼로그 처리 포함)
- 1개 삭제 후 버튼 목록 갱신하여 다음 소스 진행

**채팅 기록 삭제 (`deleteChatHistory`):**
- 소스 삭제 완료 후 자동 호출
- 더보기 버튼 순회 → "채팅" + "삭제" 텍스트 포함 메뉴 탐색
- 확인 다이얼로그 처리

---

### 2.7 커스텀 프롬프트

| 항목 | 내용 |
|------|------|
| 슬롯 수 | 2개 (커스텀 1, 커스텀 2) |
| 최대 길이 | 5,000자 |
| 저장 위치 | `chrome.storage.local` (`customPrompts` 키) |
| 편집 방법 | 모달 다이얼로그 (실시간 글자 수 카운트) |
| 실행 방식 | 프롬프트 미저장 시 → 모달 열림 / 저장됨 → 바로 실행 |
| 편집 접근 | 버튼 우상단 연필 아이콘 (hover 시 노출) |
| 삭제 | 모달 내 "삭제" 버튼 |
| 언어/분량 연동 | **미적용** — 사용자가 프롬프트에 직접 명시 |

---

### 2.8 플로팅 복사 버튼

- 자동화 완료 후 우하단에 `📋 응답 복사` FAB 자동 주입
- **1차 탐색**: `SEL.response.containers` 셀렉터 (11개 패턴)
- **2차 fallback**: `main` 내 긴 텍스트 블록 (100자 이상, 자식 요소 있는 것)
- 마지막 응답 블록의 `innerText`를 클립보드에 복사
- `navigator.clipboard` 실패 시 → `textarea` + `execCommand('copy')` fallback
- 복사 성공: 버튼 "✅ 복사 완료!" 변경 (2초)
- 응답 없음: 버튼 "⏳ 응답 없음" 변경 (2초)

---

### 2.9 UI/UX

| 요소 | 설명 |
|------|------|
| **팝업 크기** | 480px 고정 너비 |
| **디자인** | 글래스모피즘 (blur 배경 + 반투명 패널) |
| **배경 효과** | 파란색/보라색 gradient blob |
| **아이콘** | Material Symbols Outlined (로컬 번들) |
| **폰트** | Inter (로컬 번들) |
| **상태 표시** | 펄스 dot (초록=정상, 노랑=진행중, 빨강=에러) |
| **버튼 그리드** | 4×2 (8개 모드) — 색상 구분된 pill 버튼 |
| **노트북 목록** | 스크롤 가능 (max-height 160px) + 선택 하이라이트 |
| **설정 패널** | 토글 방식 (기어 아이콘 클릭) |
| **커뮤니티 링크** | 헤더 + 설정 내 카드 + 푸터 (3곳) |
| **NotebookLM 바로가기** | 헤더 외부 링크 아이콘 |

---

## 3. 아키텍처

### 3.1 파일 구조 (5개 핵심 파일)

```
manifest.json     — MV3 매니페스트, 권한 정의
background.js     — Service Worker: 메시지 중계, 탭 관리, 알림
selectors.js      — DOM 셀렉터 집중 관리 (SEL 객체)
content.js        — Content Script: NotebookLM DOM 자동화 전체
popup.js          — Popup UI 로직
index.html        — Popup HTML + 인라인 CSS
```

### 3.2 메시지 흐름

```
[YouTube Popup] → NOTEBOOKLM_AUTOMATION → [Background SW]
                                              ↓
                                         (탭 생성)
                                              ↓
[NotebookLM Content Script] ← START_AUTOMATION / CREATE_NOTEBOOK
         ↓
    DOM 자동화 실행
         ↓
[Popup] ← POPUP_STATUS ← [Background] ← [Content Script]
[Background] ← SHOW_NOTIFICATION ← [Content Script]
```

### 3.3 상태 관리

| 키 | 저장소 | 용도 |
|----|--------|------|
| `pendingTask` | `chrome.storage.session` | 자동화 작업 정보 (SW 재시작 시에도 유지) |
| `notebooks` | `chrome.storage.local` | 노트북 목록 `[{ id, title }]` |
| `selectedNotebookId` | `chrome.storage.local` | 선택된 노트북 ID |
| `notebookUrl` | `chrome.storage.local` | 수동 설정 노트북 URL |
| `customPrompts` | `chrome.storage.local` | `{ CUSTOM_1, CUSTOM_2 }` |

### 3.4 에러 핸들링

| 메커니즘 | 설명 |
|----------|------|
| `withRetry` | 자동 재시도 래퍼 (기본 2회, 2초 간격) |
| `waitForVisibleElement` | 요소 폴링 대기 (기본 15초 타임아웃) |
| `clickButtonByText` | 한/영 텍스트 동시 매칭, 가시성+활성 상태 검증 |
| 콘솔 로깅 | 모든 단계에서 `[NLM-EXT]` 프리픽스 로그 |
| 팝업 상태 동기화 | 에러 발생 시 팝업에 즉시 에러 메시지 표시 |

### 3.5 권한

| 권한 | 용도 |
|------|------|
| `tabs` | 탭 URL 확인, 탭 생성 |
| `scripting` | Content Script 동적 주입 |
| `activeTab` | 현재 탭 접근 |
| `storage` | 설정/상태 저장 |
| `notifications` | 완료 알림 |
| `*://*.youtube.com/*` (host) | YouTube URL 감지 |
| `*://notebooklm.google.com/*` (host) | NotebookLM DOM 자동화 |

---

## 4. 기술적 특이사항

### 4.1 Angular Controlled Input 우회
```js
const setter = Object.getOwnPropertyDescriptor(
  window.HTMLTextAreaElement.prototype, 'value'
).set;
setter.call(inputField, url);
inputField.dispatchEvent(new Event('input', { bubbles: true }));
```
NotebookLM은 Angular SPA로, 일반적인 `input.value = 'xxx'`로는 상태가 반영되지 않음. `HTMLTextAreaElement.prototype.value`의 native setter를 직접 호출하여 우회.

### 4.2 CDK Overlay z-index 제어
자동화 중 NotebookLM의 Angular Material 다이얼로그가 오버레이 위에 표시되도록 `.nlm-automation-active` 클래스로 z-index 강제 조정.

### 4.3 DOM 셀렉터 중앙 관리
`selectors.js`의 `SEL` 객체에 모든 DOM 셀렉터 집중. NotebookLM UI 변경 시 이 파일만 수정.

### 4.4 Service Worker 안정성
`chrome.storage.session`에 `pendingTask` 저장하여 Service Worker가 비활성화/재시작되어도 작업 정보 유지.

---

## 5. 경쟁력 분석 체크리스트

### 5.1 현재 강점

| # | 강점 | 근거 |
|---|------|------|
| 1 | **진입장벽 제로** | 설치 후 원클릭. 별도 API 키, 로그인, 설정 불필요 |
| 2 | **프라이버시 완전 보장** | 서버 전송 없음, 100% 로컬 동작 |
| 3 | **다양한 분석 모드** | 6개 프리셋 + 2개 커스텀 = 8가지 |
| 4 | **프롬프트 품질** | 각 모드별 전문가 수준의 구조화된 프롬프트 |
| 5 | **다국어 지원** | 한/영/일/중 4개 언어 |
| 6 | **분량 조절** | 간략/기본/상세 3단계 |
| 7 | **노트북 관리 통합** | 생성/선택/삭제/자동 제목 — 팝업에서 완결 |
| 8 | **완전 무료** | 사용 제한, 프리미엄 플랜 없음 |
| 9 | **가벼움** | 프레임워크 없는 Vanilla JS, 빠른 로딩 |
| 10 | **에러 복원력** | 자동 재시도, 한/영 텍스트 동시 매칭, 다단계 fallback |

### 5.2 현재 한계 / 개선 기회

| # | 한계 | 영향도 | 개선 방향 |
|---|------|--------|-----------|
| 1 | **YouTube 전용** | 높음 | PDF, 웹페이지, 텍스트 직접 입력 등 소스 확장 |
| 2 | **NotebookLM 의존** | 높음 | NotebookLM UI 변경 시 셀렉터 업데이트 필요 |
| 3 | **결과 확인 불가** | 중간 | 프롬프트 전송 후 응답 완료 감지/표시 없음 (NotebookLM에서 직접 확인) |
| 4 | **히스토리 없음** | 중간 | 이전 분석 기록 조회/재실행 기능 없음 |
| 5 | **일괄 처리 불가** | 중간 | 여러 영상을 한 번에 분석하는 기능 없음 |
| 6 | **프롬프트 공유 불가** | 낮음 | 커스텀 프롬프트 내보내기/가져오기 없음 |
| 7 | **Firefox/Edge 미지원** | 낮음 | Chrome 전용 (MV3) |
| 8 | **오프라인 불가** | 낮음 | NotebookLM 자체가 온라인 서비스 |
| 9 | **커스텀 프롬프트에 언어/분량 미적용** | 낮음 | 사용자가 직접 프롬프트에 명시해야 함 |
| 10 | **분석 결과 포맷 제한** | 낮음 | 마크다운 텍스트만 지원 (PDF, 워드 내보내기 없음) |

### 5.3 경쟁 제품 대비 포지셔닝

| 비교 축 | 피클노트 | 수동 NotebookLM 사용 | 다른 YouTube 요약 도구 |
|----------|----------|---------------------|----------------------|
| 사용 단계 | 1 클릭 | 5~10 단계 수동 | 1~2 클릭 |
| 분석 품질 | NotebookLM 엔진 (높음) | 동일 | GPT/Claude API (도구마다 다름) |
| 분석 모드 | 8가지 | 사용자 직접 작성 | 보통 1~3가지 |
| 프라이버시 | 로컬 전용 | 직접 조작 | 서버 전송 필요 |
| 비용 | 무료 | 무료 | 대부분 유료/제한 |
| 다국어 | 4개 언어 | 수동 | 영어 위주 |
| 노트북 관리 | 자동화 | 수동 | 해당 없음 |

---

## 6. 코드 품질 지표

| 지표 | 값 |
|------|-----|
| 총 JS 파일 | 4개 (background.js, content.js, popup.js, selectors.js) |
| content.js 라인 수 | ~920줄 (핵심 자동화 로직) |
| popup.js 라인 수 | ~400줄 (UI 로직) |
| background.js 라인 수 | ~150줄 (메시지 중계) |
| selectors.js 라인 수 | ~90줄 (셀렉터 관리) |
| 외부 의존성 | 0개 |
| 번들러 | 없음 |
| 테스트 | 없음 (수동 테스트) |

---

## 7. 사용 시나리오별 동작 흐름

### 시나리오 A: 최초 사용 (노트북 없음)
1. YouTube 영상 페이지에서 확장 프로그램 클릭
2. "전체 요약" 버튼 클릭
3. → 노트북 미선택 → "새 노트북 생성 중..." 상태
4. → NotebookLM 홈페이지 열림 → 만들기 버튼 자동 클릭
5. → 새 노트북 페이지 로드 → 영상 제목으로 이름 변경
6. → 5단계 자동화 실행 → 완료 알림

### 시나리오 B: 기존 노트북에 추가 (소스 삭제 O)
1. YouTube 영상 페이지에서 확장 프로그램 클릭
2. 노트북 목록에서 대상 선택
3. "기존 소스 삭제 후 추가" 체크 확인
4. "학습 노트" 버튼 클릭
5. → 해당 노트북 열림 → 기존 소스 전부 삭제 → 채팅 기록 삭제
6. → 노트북 제목 변경 → 새 소스 추가 → 프롬프트 전송

### 시나리오 C: 커스텀 프롬프트 사용
1. "커스텀 1" 버튼 클릭 (프롬프트 미저장 상태)
2. → 모달 열림 → 프롬프트 입력 (예: "이 영상을 표 형식으로 정리해줘") → 저장
3. 다시 "커스텀 1" 버튼 클릭
4. → 저장된 프롬프트로 자동화 실행

### 시나리오 D: 노트북 삭제 (상세 페이지에서)
1. NotebookLM 노트북 상세 페이지에서 확장 프로그램 클릭
2. 노트북 목록의 휴지통 아이콘 클릭
3. → Background로 삭제 요청 → 홈페이지로 이동
4. → 홈페이지 로드 후 4단계 삭제 자동화 실행
5. → 삭제 검증 → 목록 재스캔

---

## 8. 요약

피클노트 v2.0.0은 **"YouTube → NotebookLM 원클릭 자동화"**라는 명확한 가치를 제공하며, 수동으로 5~10단계를 거쳐야 하는 과정을 버튼 하나로 단축한다. 8가지 분석 모드, 4개 언어, 3단계 분량, 노트북 관리까지 팝업 하나에서 해결하며, 서버 전송 없이 100% 로컬에서 동작하는 것이 핵심 차별점이다.

가장 큰 확장 기회는 **YouTube 외 소스 지원**(PDF, 웹페이지, 텍스트)과 **분석 히스토리/일괄 처리** 기능이다. NotebookLM UI 변경에 대한 의존성은 `selectors.js` 중앙 관리로 완화했으나, 지속적 모니터링이 필요하다.
